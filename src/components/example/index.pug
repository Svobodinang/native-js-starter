mixin example(title)
    section.example
        p.h1!= `${title} rem`
        p.h1.example__title!= `${title} vw`
        .example__rects
            .example__rect
            .example__rect
        img(src='./images/mem.jpg', alt='meow', srcset='')
